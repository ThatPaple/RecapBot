let config = require('../../config.json');

/**
 * 
 * @param {*} evt  = Event (ex. AddedRecap, AddedRole....)
 * @param {*} msg  = What is logged (ex. Added a recap....)
 * @param {*} user = Who logged (ex. ThatPaple#9995)
 */
function log(evt, msg, user) {}

module.exports = log;
